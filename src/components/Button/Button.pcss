.button {
  border: none;
  font-size: 13px;
  padding: 10px;
  cursor: pointer;
  background-color: var(--main-text-color);
  color: #fff;
  border-radius: 8px;

  &--text {
    background-color: transparent;
    color: #72685d;
    font-weight: 500;
  }
}
